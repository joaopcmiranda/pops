[defaults]
inventory = inventory/hosts.yml
roles_path = roles
vault_password_file = ~/.ansible/pops-vault-password
host_key_checking = False
retry_files_enabled = False

[privilege_escalation]
become = True
become_method = sudo
