---
# Deploy or update services only (skip OS hardening).
# Usage: ansible-playbook ansible/playbooks/deploy.yml

- name: Deploy POPS services
  hosts: pops_servers
  become: true

  roles:
    - secrets
    - pops-deploy
